# The submission runs in a Linux container. Keep everything LF in the repo so
# that what you test locally on Windows is byte-identical to what Kaggle runs.
* text=auto eol=lf

# Never let Git rewrite these.
*.png binary
*.gz  binary
*.zip binary
*.parquet binary
